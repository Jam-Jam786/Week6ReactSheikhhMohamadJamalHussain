import'./Calculator.css'
import { useState } from 'react';

function Calculator()
{
    return(
        export const Calculator = () => {
            const[input, setInput] = useState('');
            const[rsult, setResults] = useState('');

            const handleClock = (value) =>{
                if(value === '='){
                    const newRes
                }
            }
        }
    )

}


export default Calculator;